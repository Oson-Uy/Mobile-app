import { spacing } from "../theme/tokens";

/** Нижний отступ списков (стандартный tab bar, не floating). */
export function buyerTabBarBottomInset(_safeBottom: number): number {
  return spacing.md;
}
