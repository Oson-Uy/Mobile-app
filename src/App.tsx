/**
 * Legacy entry — приложение запускается через expo-router (`app/_layout.tsx`).
 * Оставлено для совместимости с `index.ts`, если main снова укажут на него.
 */
export { default } from "../app/_layout";
